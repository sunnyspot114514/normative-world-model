# Runs

Per-run logs and intermediate outputs belong here. Each run should use an immutable run ID and record the code revision, config hash, dataset manifest, model revision, seeds, environment snapshot, and compute metadata.

