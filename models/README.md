# Models

Downloaded weights, quantizations, and adapters belong here and are ignored by Git. Record model IDs, revisions, licenses, hashes, and training configs in a run manifest rather than committing weights.

