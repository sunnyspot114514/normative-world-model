# Do not put secrets in this file. PowerShell scripts set all path variables.
WANDB_MODE=offline
HF_HUB_DISABLE_TELEMETRY=1
TOKENIZERS_PARALLELISM=false

# Add tokens only to an untracked `.env` when a later phase requires them.
# HF_TOKEN=
# WANDB_API_KEY=

